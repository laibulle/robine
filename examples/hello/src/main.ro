module hello
import hello.math

fn main(console: Console) -> Unit ! { Console.Write } {
    let result = hello.math.fibonacci(10);
    let message = if result == 55 { "Hello from Robine!" } else { "Broken compiler." };
    console.write_line(message)
}
